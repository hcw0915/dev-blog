# 這個站的寫作與編輯規範

文章來源是 Inkdrop，不是這個 repo —— 改 `src/pages/posts/*.md` 會被下次同步覆蓋。
要改內容一律改 Inkdrop 筆記，由 `tools/watch-publish.sh` 自動匯出、提交、部署。

## 發表流程

新文章一律先寫 `public: false`，把草稿給 Antonio 看過再翻成 `public: true`。

`public: true` 是實際的發佈開關：watcher 會在兩分鐘內把它匯出成 md、commit、push，
Vercel 接著部署，文章隨即進入 sitemap、RSS 與搜尋索引。這是掛他名字的公開發表，不是程式碼改動，
所以「寫文章」與「上線」要分開確認。

程式碼與網站功能的改動不受此限：驗證過就可以提交推送。

## 用語：台灣繁體，或直接用英文

不要用簡體圈的技術詞。常見對照：

| 不要 | 要 |
|---|---|
| 組件 | 元件 |
| 依賴（當名詞／動詞） | 相依、相依於，或直接寫 import |
| 鉤子 | hook（英文原詞），或改寫成「注入點」 |
| 隊列 | 佇列 |
| 接口 | 介面 |
| 調試 | 除錯、偵錯 |
| 對象 | 物件 |
| 代碼 | 程式碼 |
| 數據 | 資料 |
| 緩存 | 快取 |
| 默認 | 預設 |
| 數組 | 陣列 |
| 項目 | 專案 |

判斷不了就直接用英文原詞，比用錯中文好。

注意語意而不是字面取代：「覆蓋」有時指 override（→ 覆寫），有時指涵蓋範圍（→ 涵蓋），
要看上下文決定。

## 對外讀者視角

文章寫給沒看過那份程式碼的人。不要放只有進過 repo 才有意義的東西：

- **不要 diff 統計**：幾個檔案、加減幾行、某個檔案減了幾行
- **不要內部符號與檔名**：內部元件名、hook 名、私有模組路徑
- **不要內部代號**：只有團隊內部懂的工具名、專案縮寫

改成用「它在架構裡扮演的角色」描述，例如「版面組合的客戶端元件」「站台設定的 provider」。

可以保留的是讀者能對應到的東西：截圖上看得到的規模數字、通用的框架行為（Turbopack、
tree-shaking、CSS 自訂屬性的 computed 值）、以及可以帶走的判斷原則。

判準很簡單：**這句話對沒看過我們 repo 的人有沒有意義？** 沒有就刪掉或改寫。

## 圖片

- 單張圖高度不超過 50vh（版型已有全域規則，寫作時也別放超長的直式圖）
- 圖片放 `public/posts/`，檔名前綴用文章 slug，例如 `dev-panel_01-modules.png`
- 每張圖都要有具體的 alt／圖說，說明「這張在證明什麼」，不要只寫「截圖」
- 大圖先壓過再放，1000px 寬度通常就夠

## 標題與標籤

- 標題要像技術文章，不要口語（「收編」→「收斂」、「調試」→「除錯」）
- 標題要讓沒有背景的人看得懂主題，不要只寫內部專案名
- 標籤掛**技術方向**，不是內容感覺：講 Next.js 打包與 React 樹的文章掛 `React` / `Next`，
  不要因為主題跟 AI 沾邊就掛 `AI`
- 標籤同時決定文章的主題色與「相關文章」配對（見 `src/config.ts` 的 `TAG_WEIGHT`），
  掛錯會讓不相干的文章被配在一起
